name: Platform services
on:
  workflow_dispatch:
    inputs:
      target-clusters:
        type: choice
        options: ['["aks01"]', '["aks02"]', '["aks01","aks02"]']
        default: '["aks01"]'
      deploy-sequentially:
        type: boolean
        default: true
permissions: {contents: read, id-token: write}
jobs:
  platform:
    uses: MikeeeGit/aks-delivery-templates/.github/workflows/platform-services.yml@9fd0d6bd2e860c3f2e6a6aae4f154c8d3d309179
    with:
      template-ref: '9fd0d6bd2e860c3f2e6a6aae4f154c8d3d309179'
      config: platform.services.json
      environment: pprd
      region: uks
      target-clusters: ${{ inputs.target-clusters }}
      deploy-sequentially: ${{ inputs.deploy-sequentially }}
      client-id: ${{ vars.PLATFORM_CLIENT_ID }}
      runs-on: '["self-hosted","linux","aks-private"]'
